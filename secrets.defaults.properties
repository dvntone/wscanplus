# DO NOT PUT REAL SECRETS HERE.
# Developers set real values in local.properties (not committed).
#
# AI SPLIT (important):
#   GEMINI_API_KEY  = in-app AI analysis engine (Vertex AI / Google AI Pro)
#                     Used inside the Android app for WiFi threat analysis.
#                     Do NOT replace with another AI provider without maintainer approval.
#   GOOGLE_MAPS_API_KEY = Android map display + scan history heatmap
#
# Coding agents (Claude, Copilot) do NOT use these keys.
# See .env.example for desktop/dev environment keys.

GEMINI_API_KEY=REPLACE_ME
GOOGLE_MAPS_API_KEY=REPLACE_ME
